machine:
  services:
    - docker
dependencies:
  cache_directories:
        - "~/docker"
  override:
    - docker pull jrowen/dcaret 


test:
  override:
    - docker run -ti jrowen/dcaret R --version 


